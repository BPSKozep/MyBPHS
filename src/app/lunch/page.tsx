import React from "react";
import LunchOrder from "@/components/lunch/LunchOrder";

export const metadata = {
    title: "Ebédrendelés",
};

export default function Order() {
    return <LunchOrder />;
}
